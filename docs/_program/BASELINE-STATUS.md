# Baseline Status

## Current Repository State

The repository is an existing working portfolio application, not a greenfield design-only repository. Application code, routing, Firebase configuration, admin CMS modules, public sections, and deployment configuration already exist.

## Authoritative Baseline

- Public and admin application behavior: `app/`, `src/`, `firestore.rules`, and `firestore.indexes.json`.
- Project architecture and conventions: `AGENTS.md`, `.claude/CLAUDE.md`, `ARCHITECTURE.md`, and `src/pages/admin/README.md`.
- Product and deployment intent: `README.md` and `ROADMAP.md`.
- Versioning policy: `docs/VERSIONING.md`.

## Documentation Status

Before this program, `docs/` contained `VERSIONING.md` only. The phase folders created by this program are a documentation baseline for the current app and should be kept synchronized with future code changes.

## Control Rule

These docs describe the current system. They do not authorize app behavior changes, dependency changes, Firebase paid services, or source-file moves.

