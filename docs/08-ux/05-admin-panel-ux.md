# Admin Panel UX

## UX Goals

The admin panel should be efficient for repeated content management work, permission-aware, searchable, keyboard-friendly, and resilient to Firestore quota constraints.

## Core Patterns

Use DataTable, shared form components, admin layout, bulk actions, command palette, notifications, history restore, and registry-driven tabs.

## Rule

Do not duplicate CRUD or navigation behavior already provided by shared admin machinery.

